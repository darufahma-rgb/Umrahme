
## Task: Make AI Itinerary Extractor save directly to Supabase

### Overview
Refactor the "Susun Itinerary dengan AI" feature so that after the user uploads a file (PDF, Excel, CSV, image), the AI extracts all agenda items and the user can preview then confirm to save directly into the `agenda_items` table in Supabase — no manual SQL needed.

### Files to modify
- `api-server/src/routes/ai-extract-agenda.ts` — backend extraction + save endpoints
- `src/pages/admin/AdminTenantForm.tsx` — frontend preview + confirm UI

---

## PART 1 — Backend: `ai-extract-agenda.ts`

### 1A. Switch model to Claude Sonnet 4.6 via OpenRouter
Find the model string and replace it:
- Before: `"google/gemini-flash-1.5"` (or any other model)
- After:  `"anthropic/claude-sonnet-4-6"`

### 1B. Increase body size limit
In the Express app setup (likely `server.ts` or `app.ts`), find `express.json()` and increase the limit:
```
app.use(express.json({ limit: '25mb' }));
app.use(express.urlencoded({ limit: '25mb', extended: true }));
```

### 1C. Add AbortController timeout
Wrap the OpenRouter fetch call with a 60-second timeout:
```ts
const controller = new AbortController();
const timeout = setTimeout(() => controller.abort(), 60000);
try {
  const response = await fetch('https://openrouter.ai/api/v1/chat/completions', {
    signal: controller.signal,
    // ...existing options
  });
} finally {
  clearTimeout(timeout);
}
```

### 1D. Guarantee JSON response on all failures
Wrap the entire route handler in try/catch and ensure it ALWAYS returns valid JSON:
```ts
try {
  // ... handler logic
} catch (err: any) {
  if (err.name === 'AbortError') {
    return res.status(504).json({ error: 'Request timeout. Coba file yang lebih kecil.' });
  }
  return res.status(500).json({ error: err.message || 'Terjadi kesalahan.' });
}
```

### 1E. Update the AI system prompt for extraction
Replace the existing AI prompt with this exact system prompt:

```
You are an expert at reading umrah travel itineraries and extracting structured agenda data.

Extract every activity from the itinerary as a separate agenda item.

Return ONLY a valid JSON array. No markdown, no code blocks, no explanation. Just the raw JSON array.

Each item must follow this exact structure:
{
  "tanggal": "YYYY-MM-DD",
  "jam_mulai": "HH:MM",
  "judul": "string (max 60 chars)",
  "deskripsi": "string (full detail)",
  "lokasi": "string",
  "urutan": number
}

Rules:
- tanggal: parse from day headers like "DAY 1 (Kamis, 09 Juli 2026)" → "2026-07-09"
- jam_mulai: use time if stated; estimate if not:
    Subuh/Fajr → "05:00", Syuruq → "06:30", Dzuhur/Zuhur → "12:00",
    Ashar → "15:00", Maghrib → "18:00", Isya → "19:30",
    sarapan/breakfast → "07:00", makan siang/lunch → "12:30",
    makan malam/dinner → "20:00", check-out → "12:00"
- judul: concise title, max 60 characters, taken from the activity name
- deskripsi: REQUIRED, never empty. Take verbatim or closely paraphrase the
    full explanation from the itinerary. If the itinerary has a "NOTED" section
    relevant to that activity or day, include it in the deskripsi. If no
    description exists, generate a safe, factual, contextually appropriate
    description based on the activity name and location. Example: for
    "Sholat Subuh di Masjid Nabawi" write "Melaksanakan sholat Subuh berjamaah
    di Masjid Nabawi bersama seluruh rombongan." Keep it neutral and informative.
- lokasi: extract from context — hotel name, masjid name, city. If unknown, use
    the city name of that day (Makkah, Madinah, Jakarta, etc.)
- urutan: sequential per day, starting from 1 for each new date
- Include ALL activities including meals, check-in/out, transportation, prayers,
    and sightseeing — nothing skipped
```

### 1F. Parse the AI response safely
After getting the AI response text, parse it safely:
```ts
let items;
try {
  const text = data.content
    .map((i: any) => i.text || '')
    .join('');
  const clean = text.replace(/```json|```/g, '').trim();
  items = JSON.parse(clean);
  if (!Array.isArray(items)) throw new Error('Not an array');
} catch (e) {
  return res.status(500).json({ error: 'AI gagal menghasilkan format yang valid. Coba lagi.' });
}
```

### 1G. Add a separate /save endpoint
Create a new POST route `api-server/src/routes/ai-extract-agenda.ts` that handles saving:

```ts
router.post('/save', async (req, res) => {
  try {
    const { tenant_id, keberangkatan_id, items } = req.body;
    if (!tenant_id || !keberangkatan_id || !Array.isArray(items) || items.length === 0) {
      return res.status(400).json({ error: 'Data tidak lengkap.' });
    }

    const rows = items.map((item: any) => ({
      tenant_id,
      keberangkatan_id,
      tanggal: item.tanggal,
      jam_mulai: item.jam_mulai,
      judul: item.judul,
      deskripsi: item.deskripsi,
      lokasi: item.lokasi,
      urutan: item.urutan,
    }));

    const { error } = await supabase.from('agenda_items').insert(rows);
    if (error) throw error;

    return res.json({ success: true, count: rows.length });
  } catch (err: any) {
    return res.status(500).json({ error: err.message || 'Gagal menyimpan agenda.' });
  }
});
```

---

## PART 2 — Frontend: `AdminTenantForm.tsx`

### 2A. Fix JSON parse error on fetch response
Find the fetch call to the AI extract endpoint. Before calling `.json()`, add a safety check:
```ts
const res = await fetch('/api/ai-extract-agenda', { method: 'POST', body: formData });
if (!res.ok) {
  const err = await res.json().catch(() => ({ error: 'Gagal memproses file. Coba file yang lebih kecil atau format lain.' }));
  throw new Error(err.error);
}
const data = await res.json();
```

### 2B. Show preview table after extraction
After successful extraction, show a preview table (not a raw JSON dump) with columns:
- Tanggal, Jam, Judul, Lokasi

Use a simple styled table. Show the total item count: "AI berhasil mengekstrak X agenda item."

Example layout:
```tsx


  


    AI berhasil mengekstrak {items.length} agenda item. Periksa sebelum menyimpan.
  


  

    
        {items.map((item, i) => (
          
        ))}
      
Tanggal	Jam	Judul	Lokasi
{item.tanggal}	{item.jam_mulai}	{item.judul}	{item.lokasi}

  



```

### 2C. Add "Simpan X Agenda" confirm button
Below the preview table, add two buttons:
- "Ulangi" — clears the preview and lets the user upload again
- "Simpan X Agenda" (primary/blue) — calls the save endpoint

```tsx


   setExtractedItems(null)}>Ulangi
  
    Simpan {items.length} Agenda
  


```

### 2D. Implement handleSaveAgenda
```ts
const handleSaveAgenda = async () => {
  try {
    setSaving(true);
    const res = await fetch('/api/ai-extract-agenda/save', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        tenant_id: tenantId,
        keberangkatan_id: selectedKeberangkatanId,
        items: extractedItems,
      }),
    });
    const data = await res.json();
    if (!res.ok) throw new Error(data.error);
    toast.success(`${data.count} agenda berhasil disimpan!`);
    setExtractedItems(null);
    fetchAgenda(); // refresh agenda list
  } catch (err: any) {
    toast.error(err.message || 'Gagal menyimpan.');
  } finally {
    setSaving(false);
  }
};
```

---

## Expected result
Upload PDF/Excel itinerary → AI extracts all activities → preview table shown →
click "Simpan X Agenda" → langsung tersimpan ke tabel `agenda_items` di Supabase.
Hasilnya identik dengan insert SQL manual berdasarkan isi file itinerary asli.
