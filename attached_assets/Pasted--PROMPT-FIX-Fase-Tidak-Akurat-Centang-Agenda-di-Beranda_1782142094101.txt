# PROMPT FIX: Fase Tidak Akurat + Centang Agenda di Beranda

## Masalah 1 — Fase selalu "Persiapan" meski sudah di tanah suci

**Root cause:** Di `context/AuthContext.tsx`, saat app dibuka ulang dari localStorage, `hitungFaseEfektif` dipanggil dengan `null` sebagai `faseOverride` — padahal `fase_override` sudah ada di `savedTenant`:

```ts
// SEKARANG — salah, faseOverride selalu null:
fase: hitungFaseEfektif(
  null,
  savedTenant.tanggal_keberangkatan,
  savedTenant.tanggal_kepulangan,
),
```

**Fix di `context/AuthContext.tsx`** — ganti `null` dengan `savedTenant.fase_override ?? null`:

```ts
// BENAR — baca fase_override dari tenant yang tersimpan:
fase: hitungFaseEfektif(
  savedTenant.fase_override ?? null,
  savedTenant.tanggal_keberangkatan,
  savedTenant.tanggal_kepulangan,
),
```

**Catatan:** `TenantRow` sudah punya field `fase_override` di `lib/supabase.ts` — tidak perlu tambah type baru.

---

## Masalah 2 — Agenda di beranda tidak punya centang "sudah lewat"

**Root cause:** Card agenda di `components/dashboard/TravelCompanionFlow.tsx` menampilkan item tanpa indikator waktu. Di `AgendaLengkap.tsx` sudah ada logika `isPast()` yang menentukan apakah item sudah lewat berdasarkan tanggal + jam — tapi logika ini tidak dipakai di card beranda.

**Fix di `components/dashboard/TravelCompanionFlow.tsx`:**

### Langkah A — Tambah helper functions (sebelum komponen `TodayInstructionCard`)

```ts
function isToday(tanggal: string): boolean {
  return new Date().toISOString().split('T')[0] === tanggal;
}

function nowMinutes(): number {
  const now = new Date();
  return now.getHours() * 60 + now.getMinutes();
}

function jamToMinutes(jam: string): number {
  const [h, m] = jam.split(':').map(Number);
  return h * 60 + (m || 0);
}

function isPastItem(tanggal: string, jam_mulai: string | null): boolean {
  if (!isToday(tanggal)) return new Date(tanggal) < new Date(new Date().toISOString().split('T')[0]);
  if (!jam_mulai) return false;
  return nowMinutes() > jamToMinutes(jam_mulai);
}
```

### Langkah B — Update render item di `TodayInstructionCard`

Cari bagian render `visible.map((item) => ...)` dan ganti seluruh isi div item dengan versi ini yang punya centang dan styling "sudah lewat":

```tsx
{visible.map((item) => {
  const past = isPastItem(item.tanggal, item.jam_mulai);
  return (
    <div key={item.id} className="flex gap-3 items-start">
      {/* Kolom jam + centang */}
      <div className="flex w-9 flex-none flex-col items-center gap-0.5 pt-0.5">
        {past && (
          <svg viewBox="0 0 24 24" fill="none" stroke="#9ca3af" strokeWidth="2.5"
            strokeLinecap="round" strokeLinejoin="round" className="h-3 w-3">
            <polyline points="20 6 9 17 4 12" />
          </svg>
        )}
        <span
          className="font-mono text-[10px] font-bold leading-none whitespace-nowrap"
          style={{ color: past ? '#9ca3af' : 'var(--color-primary)' }}
        >
          {item.jam_mulai ? item.jam_mulai.slice(0, 5) : '-'}
        </span>
      </div>
      {/* Konten */}
      <div className="flex-1 min-w-0" style={{ opacity: past ? 0.6 : 1 }}>
        <p className="text-[12px] font-bold leading-snug"
          style={{ color: past ? '#6b7280' : '#111827' }}>
          {item.judul}
        </p>
        {item.lokasi && (
          <p className="font-mono text-[10px] text-mute mt-0.5 flex items-center gap-1">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth={2}
              strokeLinecap="round" strokeLinejoin="round" className="h-2.5 w-2.5 flex-none">
              <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"/>
              <circle cx="12" cy="10" r="3"/>
            </svg>
            {item.lokasi}
          </p>
        )}
        {item.deskripsi && (
          <p className="text-[11px] text-mute italic mt-0.5 leading-snug">{item.deskripsi}</p>
        )}
      </div>
    </div>
  );
})}
```

---

## ATURAN PENTING
- Hanya ubah 2 file: `context/AuthContext.tsx` dan `components/dashboard/TravelCompanionFlow.tsx`.
- Jangan ubah logika auth, tipe data, atau komponen lain.
- Jalankan `pnpm run typecheck` setelah selesai — pastikan nol error.